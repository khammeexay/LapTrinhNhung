void setup() {
  pinMode(9, OUTPUT);
}

void loop() {
    analogWrite(9,brightness);
    delay(30);//Wait for 30 millisecond(s)
  
    analongWrite (9, brightness);
    delay (30);//Wait for 30 millisecond(s)
  }
  
}
